const options = {
  APP: {
    SEO: {
      DEFAULT_TITLE: "Rank-Verse",
      DEFAULT_DESCRIPTION: "1v1 Matchmaking for fighting games",
      DEFAULT_URL: "https://rank-verse.netlify.app/",
    },
  },
  BACKEND: {
    API: {
      BASE_URL: process.env.BACKEND_API_URL,
    },
  },
};

export default options;
